# 📚 Полная документация P2P Chat Network

## 🎯 Общее описание

P2P Chat Network - это децентрализованная система мгновенного обмена сообщениями с криптографической безопасностью, поддержкой глобальных сетей и автоматическим обнаружением узлов.

### ✨ Ключевые особенности

- 🔐 **Криптографическая безопасность** - Ed25519 подписи всех сообщений
- 🌍 **Глобальная сеть** - поддержка пользователей со всего мира
- 🛡️ **Доверенные узлы** - только подписанные seed-узлы могут быть bootstrap
- 🔄 **Автовосстановление** - автоматическое подключение при обрывах
- 🚀 **Простота использования** - просто запусти и все подключится
- 📡 **Автообнаружение** - UDP broadcast и сканирование подсетей

---

## 🚀 Быстрый старт

### Самый простой способ (для новичков)

```bash
# Просто запусти и подключись к сети
python run.py

# Или глобальный режим для общения со всем миром
python run.py --global
```

### Для разработчика (seed-узел)

```bash
# Запуск seed-узла на d2omg.ru:6666
python run.py --seed --host 0.0.0.0 --port 6666

# Или запуск на другом порту
python run.py --seed --port 5555
```

---

## 📖 Подробное описание режимов

### 1. Автономный режим (по умолчанию)

```bash
python run.py
```

**Что делает:**
- Автоматически ищет локальные сети
- Подключается к известным bootstrap узлам
- Работает в локальной сети или через интернет

**Используемые bootstrap узлы:**
- `127.0.0.1:5555` - локальный хост
- `192.168.1.1:5555` - типичный роутер
- `192.168.0.1:5555` - альтернативный роутер

### 2. Глобальный режим

```bash
python run.py --global
```

**Что делает:**
- Ищет узлы по всему миру
- Подключается к глобальным DHT сетям
- Использует публичные реестры узлов

**Глобальные точки подключения:**
- `d2omg.ru:6666` - доверенный seed-узел
- `p2p.network:5555` - публичный bootstrap
- `node.p2p.chat:5555` - альтернативный узел
- `dht.p2p.global:5555` - глобальный DHT

### 3. Seed-режим (для серверов)

```bash
python run.py --seed
```

**Что делает:**
- Запускается как доверенный seed-узел
- Генерирует root-ключ и сертификаты
- Создает подписанную конфигурацию сети
- Принимает первые подключения клиентов

**Файлы, которые создаются:**
- `root_private.key` - приватный root-ключ
- `root_public.key` - публичный root-ключ
- `network_config.json` - конфигурация сети
- `node_seed_private.key` - ключ seed-узла

### 4. Публичный режим

```bash
python run.py --public
```

**Что делает:**
- Работает как публичный узел с белым IP
- Может принимать подключения от других
- Регистрируется в глобальных сетях
- Используется для создания mesh-сетей

### 5. Ручной режим

```bash
python p2p_app.py --host 0.0.0.0 --port 5555 --bootstrap d2omg.ru:6666
```

**Что делает:**
- Полный контроль над всеми параметрами
- Указание конкретных bootstrap узлов
- Настройка хоста и порта вручную

---

## 🔧 Подробные аргументы командной строки

### Основные аргументы

| Аргумент | Описание | Пример |
|----------|----------|--------|
| `--host` | Адрес для прослушивания | `--host 0.0.0.0` |
| `--port` | Порт для прослушивания | `--port 5555` |
| `--bootstrap` | Bootstrap узлы для подключения | `--bootstrap d2omg.ru:6666` |
| `--seed` | Запуск в seed-режиме | `--seed` |
| `--public` | Запуск в публичном режиме | `--public` |
| `--global` | Глобальный режим поиска | `--global` |
| `--no-auto-connect` | Отключить авто-подключение | `--no-auto-connect` |

### Примеры использования

#### 1. Запуск seed-узла на d2omg.ru

```bash
python p2p_app.py --seed --host 0.0.0.0 --port 6666
```

#### 2. Подключение к конкретному узлу

```bash
python p2p_app.py --bootstrap d2omg.ru:6666
```

#### 3. Запуск с несколькими bootstrap узлами

```bash
python p2p_app.py --bootstrap d2omg.ru:6666 seed1.p2p.net:5555 seed2.p2p.net:5555
```

#### 4. Запуск с отключенным авто-подключением

```bash
python p2p_app.py --no-auto-connect
```

---

## 🌐 Роли узлов в сети

### 1. Seed-узел (`seed`)

**Назначение:**
- Доверенные bootstrap узлы
- Источник правды о сети
- Подписывают конфигурации сети

**Требования:**
- Стабильное интернет-соединение
- Белый IP или проброшенный порт
- 24/7 доступность

**Особенности:**
- Имеют сертификаты от root-ключа
- Могут отзывать недоверенные узлы
- Распространяют network_config

### 2. Публичный узел (`public`)

**Назначение:**
- Узлы с белым IP
- Могут принимать подключения
- Участвуют в mesh-сетях

**Требования:**
- Публичный IP-адрес
- Проброшенный порт в роутере
- Стабильное соединение

**Особенности:**
- Не имеют специальных привилегий
- Могут стать seed-узлами при необходимости
- Помогают в маршрутизации сообщений

### 3. Клиентский узел (`client`)

**Назначение:**
- Обычные пользователи
- Подключаются к существующей сети
- Могут быть за NAT

**Требования:**
- Интернет-соединение
- Никаких специальных требований

**Особенности:**
- Автоматическое обнаружение сетей
- Поддержка NAT traversal
- Минимальные ресурсы

---

## 🔐 Криптографическая система

### 1. Root-ключевая инфраструктура

**Генерация root-ключа:**
```bash
# При первом запуске seed-режима
python run.py --seed
# Автоматически сгенерирует:
# - root_private.key (хранить офлайн!)
# - root_public.key (распространять в клиенты)
```

**Структура root-ключа:**
```json
{
  "private_key": "bytes...",
  "public_key": "base64...",
  "generated_at": 1234567890,
  "purpose": "root_authority"
}
```

### 2. Сертификаты seed-узлов

**Формат сертификата:**
```json
{
  "seed_pubkey": "base64...",
  "issued_at": 1234567890,
  "expires_at": 0,
  "sig": "signature..."
}
```

**Проверка сертификата:**
```python
# Клиент проверяет подпись root-ключом
if seed_cert.verify(root_public_key):
    print("Seed-узел доверен")
```

### 3. Network Config

**Структура конфигурации:**
```json
{
  "config": {
    "version": 1,
    "min_supported_version": "0.3.1",
    "trusted_seeds": [
      {
        "pubkey": "seed_public_key",
        "addr": "d2omg.ru:6666"
      }
    ],
    "revoked_seeds": []
  },
  "sig": "root_signature..."
}
```

**Проверка конфигурации:**
```python
# Проверяем подпись конфигурации
if network_config.verify(root_public_key):
    print("Конфигурация доверена")
```

### 4. Рукопожатие (Handshake)

**Сообщение рукопожатия:**
```json
{
  "type": "handshake",
  "node_pubkey": "base64...",
  "role": "client|public|seed",
  "version": "0.3.1",
  "timestamp": 1234567890.123,
  "signature": "node_signature..."
}
```

**Проверка рукопожатия:**
```python
# Проверяем подпись и валидность
if validate_handshake(handshake_message, node_identity):
    accept_connection()
else:
    reject_connection()
```

---

## 📡 Протоколы и порты

### 1. Основные порты

| Сервис | Порт | Протокол | Описание |
|--------|------|----------|----------|
| P2P соединения | 5555 | TCP | Основной порт для P2P соединений |
| UDP discovery | 5556 | UDP | Обнаружение узлов в локальной сети |
| HTTP API | 8080 | HTTP | API для seed-узлов (опционально) |
| Seed сервис | 6666 | TCP | Доверенный seed-узел d2omg.ru |

### 2. Форматы сообщений

#### Общее сообщение:
```json
{
  "type": "chat|ping|pong|handshake|peer_list",
  "from": "host:port",
  "timestamp": 1234567890.123,
  "message": "текст сообщения",
  "signature": "base64..."
}
```

#### Сообщение чата:
```json
{
  "type": "chat",
  "from": "d2omg.ru:6666",
  "timestamp": 1234567890.123,
  "message": "Привет, мир!",
  "signature": "signature..."
}
```

#### Ответ пинга:
```json
{
  "type": "pong",
  "from": "192.168.1.100:5555",
  "timestamp": 1234567890.123,
  "signature": "signature..."
}
```

---

## 🛠️ Администрирование

### 1. Запуск seed-узла

**Шаг 1: Генерация ключей**
```bash
python run.py --seed
# Автоматически создаст:
# - root_private.key
# - root_public.key
# - network_config.json
```

**Шаг 2: Настройка сервера**
```bash
# Запуск на сервере
python p2p_app.py --seed --host 0.0.0.0 --port 6666

# Или с использованием systemd
sudo systemctl enable p2p-seed
sudo systemctl start p2p-seed
```

**Шаг 3: Распространение публичного ключа**
```bash
# Скопировать публичный ключ для клиентов
cp root_public.key /var/www/html/p2p_pubkey.key
```

### 2. Управление сетью

**Просмотр активных узлов:**
```bash
python p2p_app.py --bootstrap d2omg.ru:6666
# В интерфейсе ввести: list
```

**Отправка сообщения всем:**
```bash
python p2p_app.py --bootstrap d2omg.ru:6666
# В интерфейсе ввести: send Hello everyone!
```

**Проверка сети:**
```bash
python p2p_app.py --bootstrap d2omg.ru:6666
# В интерфейсе ввести: ping
```

### 3. Обновление сети

**Обновление конфигурации:**
```bash
# 1. Изменить network_config.json
# 2. Подписать новым root-ключом
# 3. Распространить обновление
```

**Отзыв недоверенных узлов:**
```json
{
  "config": {
    "trusted_seeds": [...],
    "revoked_seeds": [
      "compromised_seed_pubkey"
    ]
  }
}
```

---

## 🔧 Настройка и конфигурация

### 1. Файлы конфигурации

#### network_config.json
```json
{
  "config": {
    "version": 1,
    "min_supported_version": "0.3.1",
    "trusted_seeds": [
      {
        "pubkey": "SEED_PUBLIC_KEY",
        "addr": "d2omg.ru:6666"
      }
    ],
    "revoked_seeds": []
  },
  "sig": "ROOT_SIGNATURE"
}
```

#### root_public.key
```
base64_encoded_public_key...
```

#### node_*.key
```
raw_private_key_bytes...
```

### 2. Переменные окружения

| Переменная | Описание | Значение по умолчанию |
|------------|----------|---------------------|
| `P2P_HOST` | Хост для прослушивания | `0.0.0.0` |
| `P2P_PORT` | Порт для прослушивания | `5555` |
| `P2P_ROLE` | Роль узла | `client` |
| `P2P_BOOTSTRAP` | Bootstrap узлы | `d2omg.ru:6666` |

### 3. Примеры конфигурации

#### Для разработки
```bash
export P2P_HOST=127.0.0.1
export P2P_PORT=5555
export P2P_ROLE=client
python run.py
```

#### Для продакшена
```bash
export P2P_HOST=0.0.0.0
export P2P_PORT=6666
export P2P_ROLE=seed
python run.py --seed
```

---

## 🚨 Безопасность

### 1. Best practices

#### Для seed-узлов
- Хранить `root_private.key` офлайн
- Регулярно обновлять сертификаты
- Мониторировать активность узлов
- Использовать HTTPS для API

#### Для обычных пользователей
- Проверять подписи всех сообщений
- Не подключаться к недоверенным сетям
- Использовать актуальные версии клиента

### 2. Уязвимости и защита

#### MITM атаки
- Защита: криптографические подписи
- Проверка: всегда проверять подписи сообщений

#### Подмена узлов
- Защита: доверенные seed-узлы
- Проверка: сверка публичных ключей

#### DoS атаки
- Защита: rate limiting, timeout
- Мониторинг: логирование аномальной активности

---

## 📊 Мониторинг и логирование

### 1. Логирование

**Уровни логирования:**
- `INFO` - основные события
- `WARNING` - предупреждения
- `ERROR` - ошибки
- `DEBUG` - отладочная информация

**Примеры логов:**
```
[INFO] Сервер запущен на 0.0.0.0:5555
[INFO] Подключен к seed-узлу: d2omg.ru:6666
[WARNING] Не удалось проверить подпись сообщения
[ERROR] Потеряно соединение с peer: 192.168.1.100:5555
```

### 2. Метрики

**Доступные метрики:**
- Количество активных пиров
- Скорость сообщений в секунду
- Время ответа пингов
- Объем переданных данных

**Пример мониторинга:**
```bash
# Просмотр статистики
python p2p_app.py --bootstrap d2omg.ru:6666
# Ввести: list
```

---

## 🚀 Разработка и расширение

### 1. Структура проекта

```
p2p_app/
├── p2p_app.py          # Основное приложение
├── run.py              # Утилита запуска
├── start_p2p.py        # Скрипт запуска
├── DOCUMENTATION.md     # Эта документация
├── root_private.key    # Приватный root-ключ
├── root_public.key     # Публичный root-ключ
├── network_config.json # Конфигурация сети
└── node_*.key          # Ключи узлов
```

### 2. Добавление новых функций

#### Пример: Добавление нового типа сообщения
```python
# В process_message добавить новый тип
elif msg_type == "new_message_type":
    handle_new_message(message)
```

#### Пример: Добавление нового протокола
```python
# Создать новый класс протокола
class NewProtocol:
    def __init__(self, node):
        self.node = node
    
    def process_message(self, message):
        # Логика обработки
        pass
```

### 3. Тестирование

#### Unit тесты
```bash
python -m pytest tests/
```

#### Интеграционные тесты
```bash
python tests/integration_test.py
```

---

## ❓ FAQ (Часто задаваемые вопросы)

### Q: Как подключиться к сети?
**A:** Просто запустите `python run.py` - приложение автоматически найдет сеть.

### Q: Что делать, если не находит сеть?
**A:** Попробуйте глобальный режим: `python run.py --global` или укажите bootstrap узлы вручную.

### Q: Как создать свой seed-узел?
**A:** Запустите с флагом `--seed`: `python run.py --seed --port ваш_порт`

### Q: Как проверить подпись сообщения?
**A:** Используйте встроенные утилиты криптографии или проверяйте через `root_public.key`

### Q: Можно ли использовать без интернета?
**A:** Да, для локальной сети не нужен интернет.

### Q: Как обновить версию клиента?
**A:** Скачайте новую версию и запустите. Сеть автоматически проверит совместимость.

### Q: Что делать при ошибке подключения?
**A:** Проверьте firewall, порт forwarding и доступность bootstrap узлов.

### Q: Как добавить новый seed-узел?
**A:** Обновите `network_config.json` и подпишите root-ключом.

---

## 📞 Техническая поддержка

### 1. Каналы связи

- **Issues:** GitHub Issues
- **Email:** support@d2omg.ru
- **Discord:** P2P Network Community

### 2. Сообщения об ошибках

При сообщении об ошибке предоставляйте:
- Версию приложения
- Операционную систему
- Логи ошибок
- Шаги воспроизведения

### 3. Версионирование

**Схема версионирования:**
- `MAJOR.minor.patch`
- `MAJOR` - несовместимые изменения
- `minor` - новая функциональность
- `patch` - исправления ошибок

---

## 📄 Лицензия

MIT License - свободное использование и модификация с сохранением авторских прав.

---

**🎯 Последнее обновление:** 8 декабря 2025 г.  
**📝 Версия:** 0.3.1  
**👨‍💻 Разработано для:** d2omg.ru P2P Network