#!/usr/bin/env python3
"""
Супер простой запуск P2P сети
Просто запусти и все подключится автоматически!
"""

import subprocess
import sys
import time
import os

def print_header():
    print("""
╔═══════════════════════════════════════════════════╗
║              🚀 Auto-P2P Network                    ║
║           Просто запусти и подключись!              ║
╚═══════════════════════════════════════════════════╝
""")

def auto_connect():
    """Автоматическое подключение к сети"""
    print_header()
    print("🔍 Автоматический поиск сетей...")
    print("⏳ Жду подключения к сети...")
    print("💡 Если сеть не найдена, создам новую автоматически")
    print("-" * 50)
    
    # Запускаем приложение с авто-подключением
    cmd = [sys.executable, "p2p_app.py"]
    
    try:
        # Запускаем процесс
        process = subprocess.Popen(cmd, stdout=subprocess.PIPE, stderr=subprocess.STDOUT, 
                                universal_newlines=True, bufsize=1)
        
        # Отображаем вывод в реальном времени
        for line in iter(process.stdout.readline, ''):
            print(line.strip())
            
        process.wait()
        
    except KeyboardInterrupt:
        print("\n👋 Пользователь прервал выполнение")
        process.terminate()
    except Exception as e:
        print(f"❌ Ошибка: {e}")

if __name__ == "__main__":
    auto_connect()