# Децентрализованная P2P вычислительная сеть

Проект создания горизонтальной P2P сети для распределенных вычислений с использованием compute-кредитов.

## 🎯 Цель системы

Создать **горизонтальную P2P-сеть**, в которой:

* любые узлы равноправны;
* вычислительные ресурсы (CPU/GPU/RAM/Disk) обмениваются напрямую;
* нет администраторов, центров управления и скрытых привилегий;
* некорректные и вредоносные действия невозможны на уровне протокола;
* участие измеряется в **compute-кредитах** (не деньги).

## 🏗️ Архитектура

### Компоненты системы

```
src/
├── core/                    # Ядро системы
│   ├── node.py            # Основной класс узла
│   ├── task.py            # Описание задач и их выполнение
│   └── credits.py         # Система compute-кредитов
├── sandbox/               # Изолированное исполнение
│   └── execution.py       # Sandbox для безопасного выполнения
├── reputation/            # Система репутации
│   └── system.py         # Управление репутацией узлов
├── pricing/               # Динамическое ценообразование
│   └── dynamic.py        # Адаптивное ценообразование
└── main.py               # Основное приложение
```

### Типы поддерживаемых задач

- `range_reduce` - редукция диапазонов
- `map` - параллельное отображение
- `map_reduce` - map-reduce операции
- `matrix_ops` - операции с матрицами
- `ml_inference` - ML инференс
- `ml_train_step` - шаг ML обучения (data-parallel)

## 🚀 Быстрый старт

### Установка зависимостей

```bash
pip install -r requirements.txt
```

### Запуск сети

```bash
# Запуск основного узла
python src/main.py --host 0.0.0.0 --port 5555

# Запуск с конфигурацией
python src/main.py --config config/network_config.json

# Запуск в режиме отладки
python src/main.py --debug
```

### Пример использования

```python
import asyncio
from src.main import ComputeNetwork
from src.core.task import Task

async def main():
    # Создаем сеть
    network = ComputeNetwork(host='127.0.0.1', port=5556)
    await network.start()
    
    # Создаем задачу
    task = Task.create_range_reduce(
        owner_id=network.node.node_id,
        start=1,
        end=1000,
        operation="sum"
    )
    
    # Подаем задачу
    task_id = await network.submit_task(task.to_dict())
    print(f"Задача создана: {task_id}")
    
    await network.stop()

asyncio.run(main())
```

## 📋 Конфигурация

Система конфигурируется через JSON файл `config/network_config.json`:

```json
{
  "sandbox": {
    "type": "process_isolation",
    "resource_limits": {
      "cpu_time_seconds": 30,
      "memory_bytes": 104857600,
      "file_size_bytes": 52428800
    }
  },
  "pricing": {
    "base_cpu_price": 0.01,
    "base_gpu_price": 0.05,
    "urgency_multiplier": {
      "low": 0.8,
      "normal": 1.0,
      "high": 1.5
    }
  },
  "reputation": {
    "decay_rate": 0.01,
    "weights": {
      "success_rate": 0.3,
      "task_quality": 0.25
    }
  }
}
```

## 🔧 Особенности

### 1. Безопасность

- **Sandbox исполнение**: задачи выполняются в изолированной среде
- **Запрет опасных операций**: no network, no filesystem, no shell
- **Ресурсные лимиты**: CPU, RAM, GPU, Disk ограничения
- **Валидация кода**: проверка на безопасность перед выполнением

### 2. Репутация

- **Многокритериальная оценка**: успешность, качество, последовательность
- **Старение репутации**: временной decay для устаревших данных
- **Бонусы и штрафы**: поощрение хорошего поведения
- **Контекстная оценка**: разные весы для разных типов задач

### 3. Динамическое ценообразование

- **Адаптивные цены**: на основе спроса и предложения
- **Учет репутации**: хорошие узлы дешевле
- **Срочность задач**: срочные задачи дороже
- **Рыночные условия**: low/normal/high/critical demand

### 4. Compute-кредиты

- **Безденежная система**: только compute-кредиты
- **Автоматический учет**: списание/начисление за задачи
- **История транзакций**: полный учет всех операций
- **Балансы узлов**: отслеживание кредитов

## 🧪 Тестирование

```bash
# Запуск тестов
pytest tests/

# Тесты с покрытием
pytest --cov=src tests/

# Интеграционные тесты
python tests/integration_test.py
```

## 📊 Мониторинг

### Статистика сети

```python
# Получение статуса сети
status = await network.get_network_status()
print(f"Узлы: {status['peers_count']}")
print(f"Задачи: {status['active_tasks']}")
print(f"Кредиты: {status['credits']}")
```

### Аналитика ценообразования

```python
# Получение аналитики
analytics = network.pricing_engine.get_pricing_analytics()
print(f"Рыночное условие: {analytics['market_condition']}")
print(f"Текущие цены: {analytics['current_prices']}")
```

### Репутация узлов

```python
# Получение репутации
score = await network.reputation_manager.get_reputation_score(node_id)
level = await network.reputation_manager.get_reputation_level(node_id)
print(f"Балл: {score:.3f}, Уровень: {level}")
```

## 🛡️ Безопасность

### Защита от злоупотреблений

1. **Sandbox изоляция**: задачи выполняются в ограниченной среде
2. **Ресурсные лимиты**: предотвращение DoS атак
3. **Криптографические подписи**: защита от подделки сообщений
4. **Система репутации**: штрафы за вредоносное поведение
5. **Валидация входных данных**: проверка всех запросов

### Запрещенные операции

- Сетевые подключения
- Файловая система хоста
- Системные вызовы
- Shell-команды
- Произвольный Python код

## 🌐 Сетевое взаимодействие

### Discovery

- **UDP broadcast**: обнаружение в локальной сети
- **Bootstrap узлы**: доверенные точки входа
- **Mesh-топология**: полная децентрализация

### Протокол сообщений

```json
{
  "type": "task_request|task_result|credit_transfer",
  "from": "node_id",
  "to": "node_id",
  "timestamp": 1234567890.123,
  "data": {...}
}
```

## 🔧 Администрирование

### Запуск нескольких узлов

```bash
# Узел 1
python src/main.py --port 5555

# Узел 2
python src/main.py --port 5556 --bootstrap 127.0.0.1:5555

# Узел 3
python src/main.py --port 5557 --bootstrap 127.0.0.1:5555 127.0.0.1:5556
```

### Консольные команды

```bash
# Статус сети
python src/main.py --status

# Проверка здоровья
python src/main.py --health

# Экспорт данных
python src/main.py --export-data
```

## 📈 Производительность

### Метрики

- **Latency**: время выполнения задач
- **Throughput**: количество задач в секунду
- **Resource utilization**: использование ресурсов
- **Network overhead**: сетевые издержки

### Оптимизации

- **Параллельное выполнение**: многопоточная обработка
- **Кэширование**: кэш репутации и цен
- **Сглаживание цен**: предотвращение резких колебаний
- **Адаптивное планирование**: оптимизация назначения задач

## 🚨 Отказоустойчивость

### Восстановление сети

- **Автоматическое переподключение**: при обрывах связи
- **Резервные пути**: альтернативные маршруты
- **Контрольные точки**: сохранение состояния
- **Самовосстановление**: автоматическое восстановление топологии

### Обработка сбоев

- **Таймауты**: ограничение времени выполнения
- **Повторы**: автоматическая повторная отправка
- **Откат**: откат при ошибках
- **Логирование**: полный аудит всех операций

## 📄 Лицензия

MIT License - свободное использование и модификация с сохранением авторских прав.

## 🤝 Вклад в проект

### Развёртывание

1. Форкните репозиторий
2. Создайте ветку для вашей фичи
3. Внесите изменения
4. Добавьте тесты
5. Отправьте pull request

### Сообщения об ошибках

При сообщении об ошибке предоставляйте:
- Версию системы
- Операционную систему
- Логи ошибок
- Шаги воспроизведения

## 📞 Контакты

- Issues: GitHub Issues
- Email: support@example.com
- Discord: P2P Network Community

---

**🎯 Это не блокчейн. Не маркетплейс. Не ботнет.**

> **Это распределённая вычислительная сеть с равными узлами и встроенными ограничениями на зло.**