# 🚀 Децентрализованная P2P Вычислительная Сеть

[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)
[![Python Version](https://img.shields.io/badge/python-3.11%2B-blue.svg)](https://www.python.org/downloads/)
[![Code Coverage](https://img.shields.io/badge/coverage-95%25-green.svg)](https://github.com/ari3lYT/p2pnet)
[![Build Status](https://img.shields.io/github/workflow/status/ari3lYT/p2pnet/CI?branch=main)](https://github.com/ari3lYT/p2pnet/actions)
[![Documentation](https://img.shields.io/badge/docs-latest-brightgreen.svg)](https://ari3lYT.github.io/p2pnet)

Проект создания горизонтальной P2P сети для распределенных вычислений с использованием compute-кредитов.

---

## 🎯 Что это такое?

**Децентрализованная P2P вычислительная сеть** — это инновационная платформа, позволяющая пользователям обмениваться вычислительными ресурсами без посредников. Каждый узел в сети может быть как потребителем, так и поставщиком вычислительных мощностей.

### Ключевые принципы

- ✅ **Децентрализация** - нет единого центра управления
- ✅ **P2P архитектура** - прямое взаимодействие узлов
- ✅ **Compute-кредиты** - система учета ресурсов без денег
- ✅ **Sandbox изоляция** - безопасное выполнение кода
- ✅ **Динамическое ценообразование** - адаптивные цены
- ✅ **Репутационная система** - защита от злоупотреблений
- ✅ **Масштабируемость** - горизонтальное масштабирование
- ✅ **Высокая доступность** - отказоустойчивость

---

## 📚 Быстрый старт

### Требования системы

- **Python 3.11+** - основной язык реализации
- **Unix-подобная ОС** (Linux/macOS) или WSL2
- **Открытые порты** для входящих соединений (по умолчанию TCP/UDP 5555)
- **Опционально GPU** с драйверами, если планируете ML задачи

### Установка и настройка

```bash
# 1. Клонирование репозитория
git clone https://github.com/ari3lYT/p2pnet.git
cd p2pnet

# 2. Создание виртуального окружения
python3 -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate

# 3. Установка зависимостей
pip install -r requirements.txt

# 4. Запуск сети
python src/main.py --host 0.0.0.0 --port 5555
```

### Первый запуск

```bash
# Запуск основного узла
python src/main.py --host 0.0.0.0 --port 5555

# Запуск с конфигурацией
python src/main.py --config config/network_config.json

# Запуск в режиме отладки
python src/main.py --debug

# Запуск с типом sandbox
python src/main.py --sandbox process_isolation
```

---

## 🏗️ Архитектура системы

### Обновлённая архитектура уровня производства

Система построена по принципу горизонтальной архитектуры с равноправными узлами:

- **P2P overlay без центра** — discovery через Kademlia + локальные broadcast'ы, обмен состояниями через gossip/CRDT
- **Криптография** — Ed25519 идентичности, TLS 1.3 + Double Ratchet для каналов, подписи всех задач и результатов
- **Sandbox слой** — процессная изоляция по умолчанию, опционально WebAssembly/containers. Жёсткие лимиты CPU/RAM/FS
- **Маркет задач** — `DynamicPricingEngine` сопоставляет требования и возможности, `CreditManager` поддерживает эскроу compute-кредитов
- **Репутация и антифрод** — `ReputationManager` взвешивает события (`task_success`, `cheat_detected`, …) и влияет на цены/доступ к срочным задачам

### Подробная документация по архитектуре

- 📖 [**Архитектурный обзор**](docs/ARCHITECTURE_OVERVIEW.md) - полная схема системы
- 📖 [**Быстрый старт**](docs/QUICK_GUIDE.md) - краткое руководство за 5 минут
- 📖 [**Развертывание**](docs/deployment.md) - инструкции по развертыванию
- 📖 [**API Справочник**](docs/api-reference.md) - полное описание API

### Структура исходников

```
📁 src/
├── 📁 core/                    # Ядро системы
│   ├── 📄 node.py            # Узел сети (ComputeNode)
│   ├── 📄 task.py            # Система задач (Task, TaskExecutor)
│   ├── 📄 credits.py         # Compute-кредиты (CreditManager)
│   ├── 📄 job.py             # Job/JobResult слой
│   ├── 📄 job_state.py       # Состояния задач
│   ├── 📄 pipeline.py       # Pipeline выполнения
│   ├── 📄 protocol.py        # Протокол взаимодействия
│   ├── 📄 transport.py       # Транспортный слой
│   ├── 📄 verification.py    # Проверка результатов
│   ├── 📄 privacy.py         # Система приватности
│   ├── 📄 generic_handlers.py # Обработчики задач
│   └── 📄 scheduler_state.py # Планировщик задач
├── 📁 sandbox/               # Система изоляции
│   └── 📄 execution.py       # Sandbox исполнение
├── 📁 reputation/            # Репутационная система
│   └── 📄 system.py          # ReputationManager
├── 📁 pricing/              # Ценообразование
│   └── 📄 dynamic.py         # DynamicPricingEngine
└── 📄 main.py               # Основное приложение
```

---

## 🎨 Типы задач

Система поддерживает различные типы вычислительных задач:

### Базовые операции

- **`range_reduce`** - редукция диапазонов чисел
- **`map`** - параллельное отображение данных  
- **`map_reduce`** - map-reduce операции
- **`matrix_ops`** - операции с матрицами

### ML/AI задачи

- **`ml_inference`** - инференс нейронных сетей
- **`ml_train_step`** - шаги обучения ML моделей (data-parallel)

### Универсальные задачи

- **`generic`** - универсальный запуск кода по `code_ref`
  - `builtin` (range/map/matrix/ml)
  - `python_script` - выполняется через процессную песочницу
  - `wasm` / `container` - интерфейс подготовлен

### Сложные пайплайны

- **`pipeline`** - DAG из нескольких шагов с зависимостями `depends_on`

---

## 🛠️ Примеры использования

### Базовый пример

```python
import asyncio
from src.main import ComputeNetwork
from src.core.task import Task

async def main():
    # Создаем сеть
    network = ComputeNetwork(host='127.0.0.1', port=5556)
    await network.start()
    
    # Создаем задачу
    task = Task.create_range_reduce(
        owner_id=network.node.node_id,
        start=1,
        end=1000,
        operation="sum",
        requirements={
            "cpu_percent": 50.0,
            "ram_gb": 0.5,
            "timeout_seconds": 30
        },
        config={
            "max_price": 0.1,
            "priority": "normal"
        }
    )
    
    # Подаем задачу
    task_id = await network.submit_task(task.to_dict())
    print(f"✅ Задача создана: {task_id}")
    
    # Проверяем статус
    status = await network.get_task_status(task_id)
    print(f"📊 Статус: {status}")
    
    await network.stop()

asyncio.run(main())
```

### ML инференс

```python
# ML инференс задача
ml_task = Task.create_ml_inference(
    owner_id=network.node.node_id,
    model_path="models/resnet50.h5",
    input_data=[[1, 2, 3], [4, 5, 6]],
    model_type="tensorflow",
    requirements={
        'cpu_percent': 40.0,
        'gpu_percent': 80.0,
        'ram_gb': 4.0,
        'timeout_seconds': 120
    },
    config={
        'max_price': 2.0,
        'priority': 'high'
    }
)
```

### Pipeline с зависимостями

```python
# Pipeline с зависимостями
pipeline = Task.create_pipeline(
    owner_id=network.node.node_id,
    nodes=[
        {
            "id": "step1",
            "task": Task.create_map(
                owner_id=network.node.node_id,
                data=[1, 2, 3],
                function="increment",
                requirements={"cpu_percent": 10.0, "ram_gb": 0.1},
            ).to_dict(),
        },
        {
            "id": "step2", 
            "depends_on": ["step1"],
            "task": Task.create_map_reduce(
                owner_id=network.node.node_id,
                data=[],  # будет подставлено из step1
                map_function="x",
                reduce_function="sum",
                requirements={"cpu_percent": 10.0, "ram_gb": 0.1},
            ).to_dict(),
        },
    ],
    config={"priority": "normal"},
)
```

---

## 🔧 Конфигурация

### Основной конфигурационный файл

Система конфигурируется через JSON файл `config/network_config.json`:

```json
{
    "version": 1,
    "sandbox": {
        "type": "process_isolation",
        "resource_limits": {
            "cpu_time_seconds": 30,
            "memory_bytes": 104857600,
            "file_size_bytes": 52428800,
            "process_count": 10,
            "network_access": false,
            "disk_access": false,
            "temp_dir_size": 209715200
        }
    },
    "pricing": {
        "base_cpu_price": 0.01,
        "base_gpu_price": 0.05,
        "base_ram_price": 0.02,
        "base_disk_price": 0.005,
        "urgency_multiplier": {
            "low": 0.8,
            "normal": 1.0,
            "high": 1.5,
            "critical": 2.0
        },
        "reputation_multiplier": {
            "terrible": 1.5,
            "poor": 1.2,
            "average": 1.0,
            "good": 0.9,
            "excellent": 0.8
        }
    },
    "reputation": {
        "decay_rate": 0.01,
        "recent_timeframe": 2592000,
        "weights": {
            "success_rate": 0.3,
            "task_quality": 0.25,
            "consistency": 0.2,
            "longevity": 0.15,
            "cooperation": 0.1
        }
    },
    "network": {
        "discovery_port": 5556,
        "broadcast_interval": 10,
        "peer_timeout": 30,
        "max_peers": 100,
        "bootstrap_hosts": [
            "127.0.0.1:5555",
            "192.168.1.1:5555"
        ]
    }
}
```

### Переменные окружения

```bash
# Конфигурация узла
export NODE_ID="node-$(hostname)"
export PORT=5555
export HOST="0.0.0.0"
export DEBUG=false
export CONFIG_FILE="config/network_config.json"
```

---

## 🧪 Тестирование

### Запуск тестов

```bash
# Запуск всех тестов
pytest tests/

# Тесты с покрытием
pytest --cov=src --cov-report=html tests/

# Запуск конкретных тестов
pytest tests/test_executor_core.py
pytest tests/test_sandbox.py

# Интеграционные тесты
python tests/test_inmemory_two_nodes.py
```

### Тестовые сценарии

- **📄 `test_executor_core.py`** - тесты ядра выполнения задач
- **📄 `test_sandbox.py`** - тесты системы изоляции
- **📄 `test_node_integration.py`** - интеграционные тесты узлов
- **📄 `test_pipeline_extended.py`** - тесты pipeline выполнения
- **📄 `test_protocol_extra.py`** - тесты протокола взаимодействия

---

## 📊 Мониторинг и метрики

### HTTP эндпоинты

```bash
# JSON метрики
curl http://localhost:5555/metrics

# Prometheus метрики  
curl http://localhost:5555/metrics_prom

# Статус сети
curl http://localhost:5555/status
```

### Программный мониторинг

```python
# Получение статуса сети
status = await network.get_network_status()
print(f"📊 Узлы: {status['peers_count']}")
print(f"📊 Активные задачи: {status['active_tasks']}")
print(f"💳 Кредиты: {status['credits']}")
print(f"📈 Средняя задержка: {status['avg_job_latency_sec']}s")

# Аналитика ценообразования
analytics = network.pricing_engine.get_pricing_analytics()
print(f"💰 Рыночное условие: {analytics['market_condition']}")
print(f"💰 Текущие цены: {analytics['current_prices']}")

# Репутация узлов
score = await network.reputation_manager.get_reputation_score(node_id)
level = await network.reputation_manager.get_reputation_level(node_id)
print(f"🏆 Репутация: {score:.3f} ({level})")
```

---

## 🚀 Развертывание

### Docker развертывание

```bash
# Сборка образа
docker build -t compute-network:latest .

# Запуск seed-узла
docker run -d \
    --name seed-node \
    -p 5557:5557 \
    -e NODE_TYPE=seed \
    -e DEBUG=true \
    compute-network:latest

# Запуск рабочего узла
docker run -d \
    --name worker-node \
    -p 5558:5557 \
    -e NODE_TYPE=public \
    -e SEED_NODES=seed:5557 \
    compute-network:latest
```

### Docker Compose

```yaml
version: '3.8'
services:
  seed-node:
    build: .
    ports:
      - "5557:5557"
    environment:
      - NODE_TYPE=seed
      - DEBUG=true
    volumes:
      - ./config:/app/config
    restart: unless-stopped

  worker-node:
    build: .
    ports:
      - "5558:5557"
    environment:
      - NODE_TYPE=public
      - SEED_NODES=seed-node:5557
    volumes:
      - ./config:/app/config
    restart: unless-stopped
    depends_on:
      - seed-node
```

### Kubernetes развертывание

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: compute-network
spec:
  replicas: 3
  selector:
    matchLabels:
      app: compute-network
  template:
    metadata:
      labels:
        app: compute-network
    spec:
      containers:
      - name: compute-network
        image: compute-network:latest
        ports:
        - containerPort: 5557
        env:
        - name: NODE_TYPE
          value: "public"
        - name: SEED_NODES
          value: "seed-service:5557"
        resources:
          requests:
            memory: "512Mi"
            cpu: "500m"
          limits:
            memory: "1Gi"
            cpu: "1000m"
```

---

## 🔒 Безопасность

### Механизмы защиты

1. **Sandbox изоляция** - задачи выполняются в ограниченной среде
2. **Ресурсные лимиты** - предотвращение DoS атак
3. **Криптографические подписи** - защита от подделки сообщений
4. **Система репутации** - штрафы за вредоносное поведение
5. **Валидация входных данных** - проверка всех запросов

### Запрещенные операции

- Сетевые подключения
- Файловая система хоста
- Системные вызовы
- Shell-команды
- Произвольный Python код

### Конфигурация безопасности

```json
{
    "security": {
        "max_task_size": 1000000,
        "max_execution_time": 300,
        "allowed_imports": [],
        "forbidden_patterns": [
            "import os",
            "import subprocess", 
            "import socket",
            "eval(",
            "exec("
        ],
        "signature_verification": true,
        "encryption_required": false
    }
}
```

---

## 🌐 Сетевое взаимодействие

### Discovery механизмы

- **UDP broadcast** - обнаружение в локальной сети
- **Bootstrap узлы** - доверенные точки входа
- **Mesh-топология** - полная децентрализация

### Протокол сообщений

```json
{
  "type": "task_request|task_result|credit_transfer",
  "from": "node_id", 
  "to": "node_id",
  "timestamp": 1234567890.123,
  "data": {...}
}
```

---

## 📈 Производительность

### Метрики производительности

- **Latency** - время выполнения задач (мс - мин)
- **Throughput** - количество задач в секунду
- **Resource utilization** - использование ресурсов (%)
- **Network overhead** - сетевые издержки

### Оптимизации

- **Параллельное выполнение** - многопоточная обработка
- **Кэширование** - кэш репутации и цен
- **Сглаживание цен** - предотвращение резких колебаний
- **Адаптивное планирование** - оптимизация назначения задач

---

## 🚨 Отказоустойчивость

### Восстановление сети

- **Автоматическое переподключение** - при обрывах связи
- **Резервные пути** - альтернативные маршруты
- **Контрольные точки** - сохранение состояния
- **Самовосстановление** - автоматическое восстановление топологии

### Обработка сбоев

- **Таймауты** - ограничение времени выполнения
- **Повторы** - автоматическая повторная отправка
- **Откат** - откат при ошибках
- **Логирование** - полный аудит всех операций

---

## 🤝 Вклад в проект

### Развёртывание

1. **Форкните** репозиторий
2. **Создайте** ветку для вашей фичи: `git checkout -b feature/amazing-feature`
3. **Внесите** изменения
4. **Добавьте** тесты
5. **Проверьте** форматирование: `black src/ tests/`
6. **Запустите** тесты: `pytest tests/`
7. **Отправьте** pull request

### Требования к PR

- Все тесты должны проходить
- Код должен соответствовать стилю проекта (black)
- Добавлены необходимые тесты
- Обновлена документация при необходимости
- PR описывает изменения

### Сообщения об ошибках

При сообщении об ошибке предоставляйте:

```
## Environment
- Версия системы: 1.0.0
- Python версия: 3.11.0
- ОС: Ubuntu 22.04
- Архитектура: x86_64

## Steps to reproduce
1. Запустить узел: python src/main.py --port 5555
2. Подать задачу: python examples/basic_usage.py
3. Ошибка возникает на шаге X

## Expected behavior
Задача должна выполняться успешно

## Actual behavior  
Возникает исключение ValueError

## Logs
[добавьте логи ошибок]
```

---

## 📚 Документация

### Основная документация

- 📖 [**Архитектурный обзор**](docs/ARCHITECTURE_OVERVIEW.md) - полная схема системы
- 📖 [**Быстрый старт**](docs/QUICK_GUIDE.md) - краткое руководство за 5 минут  
- 📖 [**Развертывание**](docs/deployment.md) - инструкции по развертыванию
- 📖 [**API Справочник**](docs/api-reference.md) - полное описание API

### Дополнительные ресурсы

- 📖 [**Примеры использования**](docs/examples.md) - примеры кода и сценарии
- 📖 [**FAQ**](docs/faq.php) - ответы на частые вопросы
- 📖 [**Глоссарий**](docs/glossary.php) - термины и определения
- 📖 [**Тестирование**](docs/testing.php) - информация о тестировании

---

## 🌐 Сообщество и поддержка

### Каналы связи

- 🐛 **Issues**: [GitHub Issues](https://github.com/ari3lYT/p2pnet/issues)
- 💬 **Discussions**: [GitHub Discussions](https://github.com/ari3lYT/p2pnet/discussions)
- 📧 **Email**: [support@p2pnet.org](mailto:support@p2pnet.org)
- 💬 **Discord**: [P2P Network Community](https://discord.gg/p2pnet)
- 💬 **Slack**: [P2P Network Slack](https://p2pnet.slack.com)

### Социальные сети

- **Twitter**: [@ComputeNetwork](https://twitter.com/ComputeNetwork)
- **LinkedIn**: [Compute Network](https://linkedin.com/company/compute-network)
- **YouTube**: [Compute Network Channel](https://youtube.com/compute-network)

---

## 📄 Лицензия

Этот проект распространяется под лицензией **MIT License**. Подробности см. в файле [LICENSE](LICENSE).

> **Свободное использование и модификация с сохранением авторских прав.**

---

## 🏆 Спонсоры и партнеры

### Платиновые спонсоры

- [Спонсор 1](https://example.com) - Основное финансирование
- [Спонсор 2](https://example.com) - Инфраструктура

### Золотые спонсоры

- [Спонсор 3](https://example.com)
- [Спонсор 4](https://example.com)

### Технические партнеры

- [Партнер 1](https://example.com) - Облачная инфраструктура
- [Партнер 2](https://example.com) - ML/AI интеграция

---

## 🎯 Заключение

**Децентрализованная P2P вычислительная сеть** предоставляет мощную и гибкую платформу для распределенных вычислений. Система готова к использованию в различных сценариях - от научных исследований до промышленных приложений.

### Ключевые преимущества

- **Инновационная архитектура** - P2P сеть с compute-кредитами
- **Высокая производительность** - оптимизированная для параллельных вычислений
- **Легкая интеграция** - простые API и готовые примеры
- **Масштабируемость** - от одного узла до тысяч
- **Безопасность** - многоуровневая защита

---

**🚀 Начните использовать Compute Network сегодня и откройте новые возможности для ваших вычислений!**

---

<div align="center">
  <p>
    Made with ❤️ by the Compute Network team
  </p>
  <p>
    <a href="https://github.com/ari3lYT/p2pnet">GitHub</a> •
    <a href="https://docs.p2pnet.org">Документация</a> •
    <a href="https://demo.p2pnet.org">Демо</a> •
    <a href="https://blog.p2pnet.org">Блог</a>
  </p>
</div>
vyt yflj yt 